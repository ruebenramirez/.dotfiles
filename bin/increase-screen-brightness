#!/usr/bin/python3

import subprocess

DEBUG = True


def getBright():
    return float(subprocess.run(["light"], stdout=subprocess.PIPE).stdout)


if DEBUG:
    print(getBright())
res = subprocess.run(["light", "-A", "5"])
if DEBUG:
    print(getBright())
