#!/usr/bin/env bash

xrandr --auto
if [[ $(xrandr | grep connected | grep -v disconnected | wc -l) -gt 1 ]]; then
    ~/.screenlayout/default.sh
fi

# refresh display background now that displays are updated
$(grep feh ~/.xinitrc)
